server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/user_service_db}
    username: ${SPRING_DATASOURCE_USERNAME:rashadsuleymanov }
    password: ${SPRING_DATASOURCE_PASSWORD:4424}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false