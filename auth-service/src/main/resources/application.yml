spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    username: rashadsuleymanov
    password: 4424

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false